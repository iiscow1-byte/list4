{
  "name": "all-levels-list",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs",
    "postinstall": "nuxt prepare",
    "import": "node --experimental-strip-types --no-warnings server/db/import.ts",
    "import:aredl": "node --experimental-strip-types --no-warnings server/db/import-aredl.ts",
    "diff-sheet": "node --experimental-strip-types --no-warnings server/db/diff-sheet.ts",
    "clear-temporary-levels": "node --experimental-strip-types --no-warnings server/db/clear-temporary-levels.ts"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0"
  }
}
